import { Table } from "antd";
import styled from "styled-components";

const StyledTable = styled(Table)`
    width: 100%;
    overflow: auto; 
    .hidden {
        display: none;P 
    }
`;

export { StyledTable };
